{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "eslint": "7.32.0",
    "eslint-config-custom": "^0.0.0",
    "prettier": "^2.5.1",
    "turbo": "latest"
  }
}
